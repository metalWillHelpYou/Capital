server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # На случай, если позже добавите client-side роутинг —
    # неизвестные пути отдают index.html, а не 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
